# Pre-baked override for the Codespace demo. Mounts the jobs scheduler's
# result_root into the postgres container so server-side query exports land
# on the host. Mirrors what `sdp db setup-jobs` would write.

services:
  postgres:
    volumes:
      - ./data/jobs-results:/jobs_export:rw
